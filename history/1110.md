- **질문**: `history` 기록을 날짜별 파일로 관리해 달라는 요청.  
  **답변**: `.cursorrules`에서 일자당 10개 제한 규칙을 제거하고, 매일 새로운 `MMDD.md` 파일에 정리하도록 업데이트했다고 안내.

- **질문**: 상품 목록 로딩 실패 시 에러 UI와 재시도 버튼을 보여달라는 요청.  
  **답변**: `main.js`에 에러 상태와 `loadProducts()` 재호출 로직을 추가하고, `ItemList`에서 에러 배너와 `다시 시도` 버튼을 렌더링하도록 수정해 체크리스트 요구사항을 충족했다고 보고.

- **질문**: 로딩 실패 시 화면 확인이 가능한지 문의.  
  **답변**: `loadProducts()` 실패 시 `state.productsError`가 설정되고 `ItemList`가 오류 배너와 `다시 시도` 버튼을 출력하므로 에러 화면을 바로 확인할 수 있다고 안내.

- **질문**: 브라우저에서 네트워크를 오프라인으로 전환하는 방법 문의.  
  **답변**: 크롬 개발자 도구(Network 탭 → Throttling → Offline)와 Edge·Firefox에서의 유사한 설정 방법, 네트워크 패널이 보이지 않을 때 해결법을 단계별로 안내.

- **질문**: 전체 오프라인 처리 시 브라우저가 `인터넷 연결 없음` 화면을 띄워서 앱 내 에러를 볼 수 없다는 문제 제기.  
  **답변**: 개발자 도구의 `Request blocking`으로 특정 API만 차단하거나, `Fetch/XHR` 탭에서 `Block request URL`을 활용해 상품 API 호출만 실패하도록 설정하는 방법, MSW → `worker.use()`로 실패 응답을 임시 주입하는 코드 패턴 등 대안을 안내.

- **질문**: 스크롤이 페이지 끝에 닿으면 상품을 추가로 불러오는 무한 스크롤 구현 요청.  
  **답변**: `state`에 페이지/추가 로딩 상태를 확장하고, `loadProducts()`가 append 모드를 지원하도록 수정했으며 `IntersectionObserver`로 바닥 감지 후 추가 페이지를 요청하고, 실패 시 재시도 버튼을 제공해 무한 스크롤을 구현했다고 설명.

